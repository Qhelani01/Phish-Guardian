# Phish Guardian Environment Variables
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=8080
NODE_ENV=development

# Frontend URL (for CORS)
# For development: http://localhost:8080
# For production: https://your-domain.com
# For multiple origins: http://localhost:8080,https://your-domain.com
FRONTEND_URL=http://localhost:8080

# Session Secret (use a strong random string in production)
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# VirusTotal API Key
# Get your API key from: https://www.virustotal.com/gui/join-us
VIRUSTOTAL_API_KEY=your_virustotal_api_key_here
